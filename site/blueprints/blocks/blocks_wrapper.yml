name: Blocks wrapper
preview: fields
wysiwyg: true
icon: dashboard
tabs:
  main:
    fields:
      contentBlocks:
        extends: fields/content_blocks_nested
  # --- block options --------------
  settings:
    fields:
      grid:
        extends: fields/block_settings_grid
        width: 1/2
        grow: false
      bgcolor:
        extends: fields/block_settings_bgcolor
      bordertop:
        label: Border top
        type: toggle
        width: 1/2
      borderbottom:
        label: Border bottom
        type: toggle
        width: 1/2
      paddingtop:
        extends: fields/block_settings_padding
        label: Padding top
      paddingbottom:
        extends: fields/block_settings_padding
        label: Padding bottom
